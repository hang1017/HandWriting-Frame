{
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter ./packages run build",
    "build:client": "cd packages && cd malita && pnpm run build:client",
    "dev": "pnpm -r --filter ./packages --parallel run dev"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "typescript": "^4.6.3"
  }
}
