{
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter ./packages run build",
    "build:client": "cd packages && cd malita && pnpm run build:client",
    "build:deps": "cd packages && cd malita && pnpm run build:deps",
    "dev": "pnpm -r --filter ./packages --parallel run dev"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.2",
    "@vercel/ncc": "^0.33.4",
    "dts-packer": "^0.0.3",
    "esbuild": "^0.14.38",
    "esno": "^0.16.3",
    "fs-extra": "^10.1.0",
    "minimist": "^1.2.6",
    "typescript": "^4.6.3"
  }
}
